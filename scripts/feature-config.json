{
  "directories": [
    "api",
    "components",
    "constants",
    "hooks",
    "pages",
    "schemas",
    "stores",
    "types",
    "__tests__"
  ],
  "files": [
    {
      "path": "api/{{name}}.api.ts",
      "template": "import { apiClient } from '@/shared/lib/axios';\n\nexport const get{{PascalName}}Api = async () => {\n  // Implementation here\n};"
    },
    {
      "path": "schemas/{{name}}.schema.ts",
      "template": "import { z } from 'zod';\n\nexport const {{name}}Schema = z.object({\n  id: z.string().optional(),\n});\n\nexport type {{PascalName}}Values = z.infer<typeof {{name}}Schema>;"
    },
    {
      "path": "hooks/{{name}}.queries.ts",
      "template": "import { useQuery, useMutation } from '@tanstack/react-query';\nimport { get{{PascalName}}Api } from '../api/{{name}}.api';\n\n/**\n * Server State Hooks — Quản lý TanStack Query (fetching, mutations, cache).\n */\nexport const use{{PascalName}}Query = () => {\n  return useQuery({\n    queryKey: ['{{name}}'],\n    queryFn: get{{PascalName}}Api,\n  });\n};\n\nexport const use{{PascalName}}Mutation = () => {\n  return useMutation({\n    mutationFn: async (data: any) => {\n       // API call here\n    },\n  });\n};"
    },
    {
      "path": "hooks/use{{PascalName}}Logic.ts",
      "template": "import { useForm } from 'react-hook-form';\nimport { zodResolver } from '@hookform/resolvers/zod';\nimport { {{name}}Schema, type {{PascalName}}Values } from '../schemas/{{name}}.schema';\nimport { use{{PascalName}}Query, use{{PascalName}}Mutation } from './{{name}}.queries';\n\n/**\n * Unified UI Logic Hook — Đóng gói logic điều khiển (form, navigations, actions).\n */\nexport const use{{PascalName}}Logic = () => {\n  const { data, isLoading } = use{{PascalName}}Query();\n  const mutation = use{{PascalName}}Mutation();\n\n  const form = useForm<{{PascalName}}Values>({\n    resolver: zodResolver({{name}}Schema),\n    defaultValues: {},\n  });\n\n  const handleSubmit = async (values: {{PascalName}}Values) => {\n    // Implementation here\n  };\n\n  return {\n    form,\n    data,\n    isLoading,\n    isPending: mutation.isPending,\n    onSubmit: form.handleSubmit(handleSubmit),\n  };\n};"
    },
    {
      "path": "hooks/index.ts",
      "template": "export * from './{{name}}.queries';\nexport * from './use{{PascalName}}Logic';"
    },
    {
      "path": "pages/{{PascalName}}Page.tsx",
      "template": "import React from 'react';\n\n/**\n * {{PascalName}}Page — Trang hiển thị giao diện chính của feature {{name}}.\n */\nexport const {{PascalName}}Page = React.memo(() => {\n  return (\n    <div className=\"space-y-4\">\n      <h1 className=\"text-2xl font-bold tracking-tight\">{{PascalName}}</h1>\n      <p className=\"text-muted-foreground\">Nội dung trang {{name}}</p>\n    </div>\n  );\n});\n\n{{PascalName}}Page.displayName = '{{PascalName}}Page';"
    },
    {
      "path": "pages/index.ts",
      "template": "export * from './{{PascalName}}Page';"
    },
    {
      "path": "index.ts",
      "template": "export * from './api/{{name}}.api';\nexport * from './hooks';\nexport * from './pages';\nexport * from './schemas/{{name}}.schema';\nexport * from './types/{{name}}.types';"
    }
  ]
}
